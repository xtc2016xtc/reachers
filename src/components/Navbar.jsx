import { appleImg } from '../utils'
const Navbar = () => {
  return (
    <header>
      <nav>
        <img src={appleImg} alt="苹果图标" width={14} height={18} />

        <div>
          
        </div>
      </nav>
    </header>
  )
}

export default Navbar
